{
  "name": "autoquanta",
  "version": "1.0.0",
  "description": "AutoQuanta ML Workbench - Start both frontend and backend",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\" --names \"backend,frontend\" --prefix-colors \"blue,green\"",
    "backend": "python fastapi_server.py",
    "frontend": "cd frontend && npm run dev",
    "install-all": "pip install -r requirements.txt && cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
